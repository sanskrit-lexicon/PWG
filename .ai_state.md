# Project Objective: Digitize, correct, and enrich the PWG (Großes Petersburger Wörterbuch); current focus is front-matter publication + Andhrabharati (AB) version reconciliation, alongside the long-running `<ls>` link-target program.

## ➡️ Next Steps (Queue)

- [ ] Finish/verify `prefaces/` set — confirm all 27 pages exist in de/en/ru (README lists 1–27; commits landed 1–21+ explicitly). Spot-check pages 22–27 (vol. 5–7 titles/forewords) are present and built into `pwgpref_all.*`.
- [ ] #191 — compare v1e against previous `pwg.txt` (AB reconciliation follow-up to #180/#163).
- [ ] #190 — `<fg>`/`<fgg>` markup (related to #79 Hariv. fgg bug).
- [ ] #188 — lang-tag markup for PWG (last commit 8830bc8 touched this).
- [ ] #186 — PWG tooltip cleanup (readme suggestion for `u.` tooltip in 8412240).
- [ ] #179 — docs-pass / documentation review.
- [ ] Link-target (DTB) backlog: #173, #172, #167, #166, #162, #159, #158, #150, #145, #142, #139, #137, #133, #116, #88 …

## 🚧 Current Work-In-Progress (WIP)

- [ ] **DeepSeek pilot (`deepseek_pilot/`)** — ज slice, 4 tracks (translate / lstargets / struct / ocrdiff), derived artifacts only, source untouched. **EN translate track LIVE & working: 20/20 limit-20 batch done, quality verified high (markup/Sanskrit/`<ls>` verbatim, German→EN accurate).** Provider = **OpenModel gateway**, **Anthropic `messages` protocol** at `{base}/v1/messages` (NOT chat/completions — that 404s; OpenAI format on OpenModel = Responses API). Model = **`deepseek-v4-flash`** (`deepseek-chat` does NOT exist on OpenModel → "no channel available" 404; `deepseek-v4-pro` is the stronger option). Key lives in `../IndologyScholars/.env` (pass `--env`), not `deepseek_pilot/.env`. `max_tokens` raised to 16384 + timeout 300s for long entries (e.g. jagat, 5.6k chars). Sandbox↔host TLS link is flaky (intermittent SSL EOF) — runner retries 6× w/ backoff.
  - **Go/no-go gate PASSED on limit-20** for all 3 runnable tracks: translate-EN (20/20, markup/Sanskrit verbatim), lstargets (97.5% refs resolved, splitting w/ `<ls n=>` prefix works, 4.3% honestly low-conf flagged), struct (extraction sound). Struct prompt hardened 3×: taught `<div>` flat-marker + `<ls n="prefix,">` link-split + gloss-interrupted-by-inline-tag conventions (malformed FPs 11→2 genuine), and restricted `grammar` to true POS/case labels (dropped überh./Bed./Bez./Schol. noise; kept N. pr.).
  - **Scale-up STOPPED by user mid-run** (2026-06-22). Driver `scale_full.sh` runs tracks sequentially lstargets → EN → RU → struct (workers 12, backoff trimmed to ≤3s/3 retries, MAXPASS 15, auto-resume + final dedup). Throughput ~3–8 entries/min over the flaky TLS link (workers didn't help much — gateway latency/rate-bound, not concurrency). **Resumable**: re-run `bash scale_full.sh` (env `../IndologyScholars/.env`, `--model deepseek-v4-flash`) to continue.
  - **State at stop:** lstargets **895/2404 (37.2%)**, 4569 refs, 96.3% identified, 4.4% flagged — quality matches gate. translate EN 20/2404 (seed), RU 0, struct 20/2404 (seed, final prompt). EN/RU/struct full passes never started (driver was still on lstargets). ocrdiff still deferred (needs AB v1e diffs #180/#163).
  - Input = `../csl-orig/v02/pwg/pwg.txt` (canonical .txt; pwgxml.zip 404s).
- [ ] Front-matter project (`prefaces/`): OCR transcriptions of PWG front matter (titles, forewords, abbreviation lists, addenda) in German + EN/RU translations; per-page files + consolidated `pwgpref_all.{de,en,ru}.md` via `build_combined.py`. Most recent committed work.
- [ ] PR template / repo-hygiene pass: structured engineering checklist merged with CDSL taxonomy/licence (commits a55e1c5, c54dfee); changelog v1 (8a75935); Dependabot auto-merge workflow.

## 🧠 Dev Notes & Hypotheses

- 2026-06-21: Working tree clean except `.claude/settings.local.json` (local, untracked-relevant). Branch `master`.
- Two parallel tracks are live: (1) **content/front-matter** publishing under `prefaces/`, (2) **markup/link-target** corrections under `pwgissues/` + AB version merge (#163/#180/#191).
- AB = Andhrabharati alternate digitization of PWG; #180 aligned `<ab>` tags, #163 tracks merging AB's full version, #191 diffs v1e vs prior.
- Conventions reminder: corrections to `pwg.xml` are never edited directly — always change files via `updateByLine.py`. Issues #89 and #99 are admin noise (skip).

## ✅ Completed (Recent only)

- 06-07-2026: /cologne-pages-landing — SEO landing + .nojekyll live
- [x] Consolidated single-file front-matter editions de/en/ru + `build_combined.py` (b416b15).
- [x] PWG front-matter OCR + EN/RU translations, per-page (018fb2a).
- [x] PR template → structured checklist + CDSL taxonomy/licence merge (a55e1c5, c54dfee).
- [x] Release changelog v1 (8a75935).
- [x] #188 lang markup pass (8830bc8); #186 tooltip cleanup (9155af6, 8412240).
- [x] #180 `<ab>` alignment finished; AB v1 vs Andhrabharati v1e diff (2ac768a).
