Regex replacements for Hemachandra's Abhidhanachintamani.
It was not done by any python script.
Segregated via `cat` and `grep` and did generic regex replacements.
Whatever remained were manually corrected.
